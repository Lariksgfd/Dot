import "../stdlib/parser.dot"
import "../stdlib/lexer.dot"

pub fn main() {
    l = Lexer.new("x = 42", "<input>")
    p = Parser.new(l)
    prog = p.parse_program()

    if p.errors.len == 0 {
        print("OK: parser happy path")
    } else {
        print("ERR: parser errors")
    }
}
